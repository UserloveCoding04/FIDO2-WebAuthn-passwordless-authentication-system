###SETUP
1. Dowload MySQL and create user `root` password `123456` (or you can change as your password in the code).
2. Create new database named: fido2 (You can use MySQL Workbench): CREATE DATABASE fido2;
3. Run at app.py: http://localhost:5000

###NOTE
There are some error you might be seen: packages/ libraries (in app.py), enrionment .venv
Make sure to install those things.
